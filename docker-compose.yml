version: "3.9"

services:
  ufc-bot:
    build: .
    container_name: ufc_telegram_bot
    restart: unless-stopped
    environment:
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
    volumes:
      - ./logs:/app/logs
